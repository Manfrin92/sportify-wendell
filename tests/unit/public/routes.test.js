import { jest, expect, describe, test } from "@jest/globals";

describe("#Routes", () => {
  beforeEach(() => {
    jest.resetAllMocks();
    jest.clearAllMocks();
  });

  test("my test", () => {
    expect(true).toBeTruthy();
  });
});
